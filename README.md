# Android-Drawable-Converter
Upload a 12x12 image. Each pixel is converted into a path in a VectorDrawable XML file, so no separate PNG is needed - just drop the .xml straight into drawable folder.
Wrench icon by Icons8.